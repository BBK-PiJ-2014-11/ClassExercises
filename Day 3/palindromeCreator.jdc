// char c notes charcters first to last, char f reverses it

boolean palin = true;

String invert = "";
println("Enter a line");
String line = System.console().readLine();
println("");
int len = line.length();

for (int i = 0; i < len; i++){
	char c = line.charAt(i);
	//println (c);
	invert = c + invert;
	
}

println(line+invert);

